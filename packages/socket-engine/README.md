# wowi socket engine server

```
zip -r deploy.zip . -x "**node_modules**" "package-lock.json" ".git**" "src/**"
```

```
$ docker build . -t 676084870440.dkr.ecr.ap-northeast-2.amazonaws.com/wowi
```
